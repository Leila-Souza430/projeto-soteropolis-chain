[pytest]
; Makes soteropolis-backend/ (this file's directory) importable as the
; project root regardless of how pytest is invoked (`pytest`, `python -m
; pytest`, different CWDs) - tests/conftest.py does `from main import app`
; etc., which needs main.py's directory on sys.path.
pythonpath = .
testpaths = tests
