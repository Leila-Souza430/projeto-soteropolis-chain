<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- FastAPI backend + Supabase HTTP calls. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Geofencing anti-fraude: GPS fix captured at camera shutter instant. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Descarte capture screen: live preview only, no gallery access. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="true"/>
    <application
        android:label="Soterópolis Chain"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Web3Auth (MetaMask Embedded Wallets) login redirect callback.
                 scheme+host here MUST match Env.web3authRedirectUrl exactly
                 (lib/config/env.dart) - this is static XML, so it can't read
                 that Dart constant; if the applicationId ever changes, update
                 both together. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="soteropolisapp" android:host="com.soteropolis.soteropolis_app"/>
            </intent-filter>
            <!-- Supabase Auth magic-link email login callback. scheme+host
                 here MUST match Env.supabaseAuthRedirectUrl exactly
                 (lib/config/env.dart) - a distinct host from the Web3Auth
                 filter above (both share the soteropolisapp:// scheme) so
                 this callback is never ambiguous with that one. Also
                 requires a matching Redirect URL entry in the Supabase
                 dashboard (Authentication -> URL Configuration). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="soteropolisapp" android:host="supabase-auth-callback"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
